# GitHub Personal Access Token
# 访问 https://github.com/settings/tokens 生成
# 只需要勾选 "gist" 权限
GITHUB_TOKEN=your_github_token_here

# 是否重用同一个 Gist（true/false）
# true: 更新同一个 Gist，订阅链接不变
# false: 每次创建新的 Gist（默认）
REUSE_GIST=false

# 将此文件复制为 .env 并填入你的真实 Token